name: Publish HTML Slides

on:
  push:
    branches: [ main ]

# 強制使用 Node 24 運行，解決 Node 20 棄用警告
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Emacs & Maxima
        run: |
          sudo apt-get update
          sudo apt-get install -y emacs-nox maxima

      - name: Generate HTML Slides
        run: |
          mkdir -p ~/.emacs.d
          emacs --batch -l publish.el calculation.org -f org-reveal-export-to-html

      - name: Setup Reveal.js
        run: |
          if [ ! -d "reveal.js" ]; then
            git clone --depth 1 https://github.com/hakimel/reveal.js.git
          fi

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
          publish_branch: gh-pages
          # 修正警告：將 'nojekyll' 改為 'disable_nojekyll: false' 或 'enable_jekyll: false'
          # 在 peaceiris/actions-gh-pages 中，預設就會產生 .nojekyll。
          # 如果你想明確關閉 Jekyll，請使用：
          enable_jekyll: false
